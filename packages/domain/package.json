{
  "name": "@retrieval-agent/domain",
  "version": "0.1.0",
  "description": "Deterministic retrieval controller, replay reducer, and evidence context policy",
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "exports": {
    ".": {
      "types": "./lib/index.d.ts",
      "default": "./lib/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": ["lib", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@retrieval-agent/contracts": "workspace:*",
    "@retrieval-agent/retrieval-replay": "workspace:*",
    "@retrieval-agent/retrieval-policy": "workspace:*"
  },
  "publishConfig": { "access": "public" },
  "license": "MIT"
}
